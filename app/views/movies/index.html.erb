<% provide :page_title, "Home"%>
<% currently_at 'Home'%>

<h1 class="mb-4">Home</h1>

<% if user_signed_in? %>
  <%= link_to 'New movie', new_movie_path, class: 'btn btn-primary btn-lg mb-3' %>
<% end %>

<%= pagination @pagy %>

<% @movies.each do |movie| %>
  <article class="card mt-3 mb-3">
    <div class="card-body">
      <div class="for_photo">
        <img src="#" alt="Image" >
      </div>
      <h4><%= link_to movie.title, movie_path(movie) %></h4>
      <h4><%= truncate strip_tags(movie.body), length: 150, omission: '... (continued)'%> </h4>
        <%= render 'shared/votes', object: movie %>
      <h4>
        <% movie.categories&.each do |category| %>
          <%= link_to category.name, movies_path(category: category.name) %>
        <% end %>
      </h4>
    </div>
  </article>
<% end %> 

<%= pagination @pagy %>

